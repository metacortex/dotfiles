# ~/.zsh_env.template
# 이 파일을 복사해서 ~/.zsh_env 로 사용하세요.
# 민감한 정보는 절대 git에 커밋하지 마세요!

#############################
# ✅ 개인 환경 변수 (기본 설정)
#############################

# 컬러 출력 설정
export CLICOLOR=1
alias ls='ls -F'

# 기본 편집기 설정 (vi / nano / code -w 중 택1)
export EDITOR=vi
export GIT_EDITOR=vi

# UTF-8 인코딩 설정 (한글, 이모지, tmux 안정용)
export LANG="en_US.UTF-8"
export LC_ALL="en_US.UTF-8"
export LC_CTYPE="en_US.UTF-8"

#############################
# 🔒 민감한 토큰/키 (직접 입력)
#############################

# GitHub Personal Access Token
export GITHUB_TOKEN="your_github_token_here"

# Google Cloud 서비스 계정 키 (JSON 파일 경로)
export GOOGLE_APPLICATION_CREDENTIALS="$HOME/.secrets/your-credentials.json"

# 기타 예시 (필요 시 주석 해제)
# export OPENAI_API_KEY="your_openai_key"
# export STRIPE_SECRET_KEY="your_stripe_key"
